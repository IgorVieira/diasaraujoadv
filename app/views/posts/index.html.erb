<div class="section">
 
  <div class="row">
   

    <div class="col s12 m7 card">
        <% @posts.each do |post| %>
            <div class="card-content">
                <h3 class="gray-text"><%=post.title%></h3>
                <p class="gray-text"><%= truncate(raw(post.body), :length => 150) %></p>
            </div>
            <div class="card-action">
                <p class="date"><%= post.created_at.strftime("%b %d, %Y")%></p>

                <%= link_to "Mais", post, class:"waves-effect waves-light brown  btn"%>
            </div>
        <% end %>

        <%= will_paginate @posts %>
    </div>


   <div class="col s12 m4 card board-post">
     <div class="card brown">
        <div class="card-content">
            <h4 class="center white-text">Outros artigos</h4>
        </div>
     </div>
        <% @other_posts.each do |o| %>
            <ul>
                <i class="fa fa-bookmark-o" aria-hidden="true"></i>  <%= link_to o.title, o %>
            </ul>
        <%end%>
   </div>
    

   <div>
    
   </div>

  </div>


</div>